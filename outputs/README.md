# Outputs

Generated samples go here.

- `samples.txt` / `samples.json` — output from `python src/model_runner.py`

Run the base model:
`python src/model_runner.py`

Run the fine-tuned model:
`python src/model_runner.py --finetuned`
